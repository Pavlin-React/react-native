import React, { useState } from "react";
import {
  Button,
  FlatList,
  Linking,
  RefreshControl,
  ScrollView,
  SectionList,
  StyleSheet,
  Text,
  View,
} from "react-native";

  return (
    
  )
}

const styles = StyleSheet.create({
  body: {
    flex: 1,
    backgroundColor: "#ffffff",
  },
  text: {
    margin: 10,
    color: "#000000",
    fontSize: 45,
    fontStyle: "italic",
  },
});
